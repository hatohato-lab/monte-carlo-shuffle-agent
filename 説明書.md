# 説明書 — monte-carlo-shuffle-agent

このリポジトリが何であるかを、専門用語をできるだけ使わずに説明します。

## 一言でいうと

「カードの束をよく切る（シャッフルする）ようにリストを並べ替えるプログラム」と、
「そのシャッフルが本当に公平かどうかを、何千回も試して統計で確かめる採点プログラム」の組です。

## 何の役に立つのか

シャッフルの結果は毎回変わります。正解が1つに決まらないため、
「答えと一致するか」を見る普通のテストでは、正しさを確かめられません。

そこで本リポジトリでは、結果を1回だけ見るのではなく、
何千回も実行した集計が「公平ならこうなるはず」という統計の性質に
合っているかどうかで、合格・不合格を判定します。

サイコロにたとえると、1回振った目だけでは歪みは分かりませんが、
何千回も振って目の出方を数えれば、歪んだサイコロを見つけられます。
それと同じ考え方です。

この「統計で採点する」仕組みは、シャッフルに限らず、
結果が毎回変わるプログラムの品質確認に応用できます。

## 正しさをどう確かめているか

採点プログラム（eval/oracle.py）は、次の3点を確かめます。

1. 決まりを守っているか（妥当性）
   出てきた並びが、元の要素をちょうど1回ずつ使った「並べ替え」に
   なっているかを毎回確認します。要素が欠けたり増えたりしたら不合格です。
   渡したリストそのものを書き換えていないか、要素に重複がある場合や、
   空のリスト・要素1個のリストでも正しく動くかも確認します。

2. どの位置も公平か
   何千回も実行し、「どの要素も、どの位置にも、ほぼ同じ回数現れる」
   ことを、偏りの大きさを数値にする統計の方法（カイ二乗検定）で確認します。

3. 並び方全体が公平か
   要素4個の並び方は全部で24通りあります。その24通りが
   ほぼ同じ回数ずつ出ることも確認します。
   位置ごとには公平に見えても、実際には数種類の並びしか出さない
   実装は、ここで見つかります。

さらに、採点プログラム自身の信頼性も確認できるようにしています。
正しいシャッフルには合格を、わざと壊した4種類の実装には不合格を
出せることを、自己検証の実行（--selftest）で確かめられます。

## フォルダとファイルの案内

- README.md … 使い方の説明（技術者向け）
- 説明書.md … この文書
- eval/oracle.py … 採点プログラム本体
- eval/corpus/reference.py … お手本になる正しいシャッフル
- eval/corpus/broken_〜.py … わざと壊した実装4種（採点プログラムの動作確認用）
- .claude/agents/ … シャッフルを実装するAIエージェントへの指示書
- design/design.md … 設計の考え方の記録
- .github/workflows/ … 変更のたびに自己検証を自動実行する設定

## 用語の小辞典

- シャッフル … 要素の並び順をランダムに入れ替えること。カードを切る操作にあたります。
- オラクル … 出力の合格・不合格を判定する採点係のこと。ここでは eval/oracle.py を指します。
- モンテカルロ … 何度も試行し、その集計から性質を調べるやり方の総称です。
- カイ二乗検定 … 観測された回数の偏りが「偶然の範囲」か「偏りすぎ」かを数値で判定する統計手法です。
- 種（シード） … 乱数の出方を決める初期値。固定すると同じ結果を何度でも再現できます。
